// @flow

import type { Node as ReactNode } from 'react';

import type { styleRulesType } from '../../styles/styleTypes';
import type { Type } from '../Button.types';
import type { AccessibilityRole, EventHandler } from '../../common/common.types';

export type TouchableTypes = {|
  // Common API
  +accessibilityLabel?: string,
  +accessibilityRole?: AccessibilityRole,
  +bordered?: boolean,
  +children?: ReactNode,
  +dataTest?: string,
  +disabled?: boolean,
  +onClick: EventHandler,
  +style: any, // @FlowFixMe - the RN flow types not match the useStyle hook output properly.
  +title?: string,
  +type: Type,
  +ref?: () => {| current: ?HTMLElement |},

  // Web API
  +ariaControls?: boolean,
  +ariaExpanded?: boolean,
  +component?: string | ReactNode,
  +submit?: boolean,
  +tabIndex?: string,
  +href?: string,
  +external?: boolean, |};


export type WebComponentProps = {|
  +'aria-controls'?: boolean,
  +'aria-expanded'?: boolean,
  +'aria-label'?: string,
  +'data-test'?: string,
  +tabIndex?: string,
  +children?: ReactNode,
  +disabled?: boolean,
  +href?: string,
  +onClick: EventHandler,
  +onMouseEnter: EventHandler,
  +onMouseLeave: EventHandler,
  +role?: AccessibilityRole,
  +style: styleRulesType[],
  +target?: '' | '_blank',
  +type: 'submit' | 'button',
  +title?: string,
  +rel: ?'noopener noreferrer', |};