// @flow

import { Platform } from 'react-native';

import type { themeType } from '../ThemeProvider/defaultTheme';
import type { styleSheetType } from '../styles/styleTypes';

// eslint-disable-next-line no-unused-vars
export default function generateClickableStyles(theme: themeType): styleSheetType {
  return {
    clickablecursor_true: {
      ...Platform.select({
        web: {
          cursor: 'pointer' } }),


      textDecorationLine: 'none' } };


}