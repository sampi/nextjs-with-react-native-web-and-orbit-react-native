// @flow

import { baseURL } from '../CountryFlag.consts';

export default function getCountryFlag(code: string, width: number, widthMultiplier: number = 1) {
  return `${baseURL}/flags/${width * widthMultiplier}x0/flag-${code.toLowerCase()}.jpg`;
}