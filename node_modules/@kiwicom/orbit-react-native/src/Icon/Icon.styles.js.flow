// @flow

import type { themeType } from '../ThemeProvider/defaultTheme';
import type { styleSheetType } from '../styles/styleTypes';

// eslint-disable-next-line no-unused-vars
export default function generateIconStyles(theme: themeType): styleSheetType {
  const rules = {
    base_icon: {
      fontFamily: 'orbit-icons' },

    coloricon_currentcolor: {} };

  return rules;
}