"use strict";

var ucCase = require('../../common/utils').ucCase;

module.exports = {
  output: 'src/Icon/icons.export.generated.js',
  input: 'static/icons.json',
  generate: function generateAggregatedIconExports(iconList, HEADER) {
    return "// @flow\n\n".concat(HEADER, "\n\nimport { icons } from './index';\n").concat(Object.keys(iconList).map(function (dashCaseIconName) {
      var ucCaseIconName = ucCase(dashCaseIconName);
      return "\nconst ".concat(ucCaseIconName, " = icons.").concat(ucCaseIconName, ";\n").concat(ucCaseIconName, ".displayName = '").concat(ucCaseIconName, "';\nexport { ").concat(ucCaseIconName, " };");
    }).join('\n'), "\n");
  }
};