// @flow

import { createElement } from 'react-native-web';

import type { TextProps } from './Text.types';

export function PlatformText({ as = 'p', ...props }: TextProps) {
  return createElement(as, props);
}

// Display name for Storybook
PlatformText.displayName = 'PlatformText';