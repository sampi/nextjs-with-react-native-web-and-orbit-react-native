// @flow

import type { Node as ReactNode } from 'react';

import type { textAlignType, sizeType, colorType, fontWeightType, spaceAfterType } from '../styles';

export type TextProps = {|
  type?: colorType,
  size?: sizeType,
  weight?: fontWeightType,
  align?: textAlignType,
  uppercase?: boolean,
  italic?: boolean,
  spaceAfter?: spaceAfterType,
  dataTest?: string,
  children?: ReactNode,
  as?: 'p' | 'span' | 'div',
  id?: string, |};