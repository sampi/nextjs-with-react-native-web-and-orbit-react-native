// @flow

import type {
Node as ReactNode,
ComponentType as ReactComponentType,
Element as ReactElement } from
'react';

import type { sizeType } from '../styles';
import type { EventHandler } from '../common/common.types';

export type TextLinkType = {|
  // Common
  +children: ReactNode,
  +dataTest?: string,
  +href?: string,
  +icon?: ReactElement<any>,
  +onClick?: EventHandler,
  +size?: sizeType,
  +tabIndex?: string,
  +type?: 'primary' | 'secondary',
  // WEB only
  +asComponent?: string | ReactComponentType<any>,
  +external?: boolean,
  +rel?: string, |};


export type NativeTextLinkContainer = {|
  +children: ReactNode,
  +testID?: string,
  +onPress?: EventHandler,
  +accessibilityRole: 'button' | 'link', |};


export type WebTextLinkContainer = {|
  +children: ReactNode,
  +dataTest?: string,
  +onClick?: EventHandler,
  +accessibilityRole: 'button' | 'link',
  +href?: string,
  +target?: string,
  +tabIndex?: string,
  +asComponent?: string | ReactComponentType<any>, |};