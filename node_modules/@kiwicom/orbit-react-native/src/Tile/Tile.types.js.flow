// @flow

/*
  DOCUMENTATION: https://orbit.kiwi/components/tile/
*/
import * as React from 'react';

import type { styleRulesType } from '../styles/styleTypes';
import type { Globals, onClickEvent } from '../common/common.types';

export type Props = {|
  +title?: string,
  +description?: React.Node,
  +icon?: React.Node,
  +children?: React.Node,
  +external?: boolean,
  +href?: string,
  +onClick?: (e: onClickEvent) => void | Promise<any>,
  +initialExpanded?: boolean,
  ...Globals, |};


export type TileExpendableState = {|
  contentHeight: number |};


export type TileExpendableProps = {|
  +expanded?: boolean,
  +initialExpandedUpdated?: boolean,
  +children: React$Node, |};


export type TileHeaderProps = {|
  +icon?: React$Node,
  +description?: React$Node,
  +external?: boolean,
  +isExpandable: boolean,
  +isExpanded?: boolean,
  +title?: string,
  +onKeyPress?: (SyntheticKeyboardEvent<HTMLElement>) => void, |};


export type IconProps = {|
  +icon: React$Node |};


export type IconRightProps = {|
  +external?: boolean,
  +isExpandable?: boolean,
  +isExpanded?: boolean, |};


export type TileContainerProps = {||};
export type ClickableProps = {|
  +children: React$Node,
  +onPress?: (SyntheticKeyboardEvent<HTMLElement>) => void,
  +onClick?: () => void,
  +href?: string,
  +target?: string,
  +style?: styleRulesType[], |};


declare export default React$ComponentType<Props>;