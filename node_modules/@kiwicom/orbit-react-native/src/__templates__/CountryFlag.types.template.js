"use strict";

module.exports = {
  output: 'src/CountryFlag/CountryFlag.types.generated.js',
  input: 'src/CountryFlag/CountryFlag.consts.js',
  generate: function generateCountryFlagCodesTypes(_ref, HEADER) {
    var CODES = _ref.CODES;
    return "// @flow strict\n\n".concat(HEADER, "\n\nexport type CountryFlagCode =\n  | '").concat(CODES.join("'\n  | '"), "';\n");
  }
};