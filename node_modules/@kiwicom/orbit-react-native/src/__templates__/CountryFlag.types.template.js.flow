// @flow strict

module.exports = {
  output: 'src/CountryFlag/CountryFlag.types.generated.js',
  input: 'src/CountryFlag/CountryFlag.consts.js',
  generate: function generateCountryFlagCodesTypes(
  { CODES }: { CODES: string[], ...},
  HEADER: string)
  {
    return `// @flow strict

${HEADER}

export type CountryFlagCode =
  | '${CODES.join("'\n  | '")}';
`;
  } };